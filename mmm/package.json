{
  "name": "mmm",
  "version": "0.1.0",
  "private": true,
  "description": "Mmm! (Morning Motion Multivitamin) — personalization engine, Shopify theme overlay, and integrations. See CLAUDE.md.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
